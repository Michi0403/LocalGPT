# LocalGPT 4.7.2 source validation

## Constraints

- LocalGPT 4.7.1 is the immediate supplied source baseline.
- The supplied runtime log, rendered HTML and screenshots were used as local diagnostic evidence.
- No GitHub/network repository content was used.
- No `dotnet`, restore, NuGet, MSBuild, build, publish or installer command was invoked in this environment.
- Validation is static/source-level and does not claim a compiled runtime result.

## Verified source contracts

- Version-slot policy is respected and LocalGPT/WebView/installer source identities are synchronized at 4.7.2.
- Provider-bound Council roles reconcile equivalent current participant identities instead of relying on raw selection-key string equality, while ambiguous cross-host same-name models remain rejected.
- Explicit team model bindings are protected from automatic one-run health exclusion so an approved exclusion cannot invalidate the role assignment that made the model part of the run.
- Assigned single-model workflow steps and assigned role-result synthesis use the same safe identity reconciliation.
- Kernel Creature Tournament live battle rounds project the current two fighter IDs, restrict model calls to that pair, use benchmark-bounded parallel execution, and send bounded recent battle context.
- Tournament ASCII output uses the enlarged 168x44 session surface, wider roster labels, richer actor rigs, generated per-exchange HUD/effect motifs, trainer/creature statements, and match-opening greeting/attack/finisher animation phases.
- Existing Pixel rendering remains wired to the deterministic frame stream.
- The Chat game modal uses responsive viewport sizing, avoids an independent header scrollbar, and raises DevExpress dropdown portals above the open modal.
- Existing `@rendermode InteractiveServer` component coverage is byte-for-byte unchanged as a file set from the supplied 4.7.1 baseline.
- Python release/game/provider/chat audits, XML/JSON parsing, source lexical delimiter checks, archive CRC/path checks and clean extraction comparison are used before delivery.
