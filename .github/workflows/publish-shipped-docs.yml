name: Publish LocalGPT Kawaii documentation

on:
  push:
    paths:
      - '.github/workflows/publish-shipped-docs.yml'
      - '.github/scripts/prepare-pages-artifact.py'
      - 'LocalGPTWebviewWrapper/LocalGPT/wwwroot/help-docs/**'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: localgpt-github-pages
  cancel-in-progress: true

jobs:
  build:
    name: Validate and package Kawaii documentation
    if: github.event_name == 'workflow_dispatch' || github.ref_name == github.event.repository.default_branch
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository documentation
        uses: actions/checkout@v6

      - name: Prepare exact in-app documentation tree
        shell: bash
        run: |
          set -euo pipefail
          python3 .github/scripts/prepare-pages-artifact.py \
            --source LocalGPTWebviewWrapper/LocalGPT/wwwroot/help-docs \
            --output pages-artifact

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Kawaii documentation artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: pages-artifact

      - name: Summarize artifact
        shell: bash
        run: |
          set -euo pipefail
          {
            echo '## LocalGPT Kawaii documentation'
            echo
            echo 'The workflow publishes the checked-in documentation used by the app.'
            echo
            echo '```json'
            cat pages-artifact/github-pages-deployment.json
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

  deploy:
    name: Deploy GitHub Pages
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Kawaii documentation
        id: deployment
        uses: actions/deploy-pages@v4
