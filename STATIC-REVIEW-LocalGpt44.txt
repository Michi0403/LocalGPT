LocalGPT 2.1.22 static review
===============================

Implemented scope
-----------------
- PowerShell StrictMode-safe compiler XML fallback for DocFX metadata failures.
- Documentation target remains enabled after normal LocalGPT builds.
- Direct installer quick starts force a fresh /chat load with team, preset and maintained starter key.
- Chat applies the requested Council team/preset, clears the Council conversation and submits the maintained pre-prompt.
- Direct starter preparation closes restored configuration/details workspaces so they cannot remain as an invisible click blocker.
- Open Chat configuration workspaces are explicitly opaque and interactive.
- Team-owned pre-prompts are shown as direct Council starters in Chat.
- Form-heavy DevExpress dialogs use the available viewport while the existing Chat configuration layout remains unchanged.
- Installer removes duplicate LocalGPT URL/LNK shortcuts only after resolving that they target the active AppData installation.
- Test Lab remote-import checkboxes are side-by-side, explicitly labelled and explain the required review approval.

Static validation performed in this environment
-----------------------------------------------
- Async continuation audit passed for 140 source files and 2,023 await tokens.
- Application architecture audit passed.
- JavaScript syntax check passed with Node.js 22 for localgpt-chat-ui.js.
- 24 JSON files parsed; English and German catalogs contain 1,375 matching keys.
- XML, project, props and targets files parsed successfully.
- Maintained-source merge-marker scan passed.
- Lightweight lexical closure check passed for all C# files.
- No bin, obj, .pio, DLL, EXE, PDB, OBJ or firmware BIN artifacts are included.

Limitations
-----------
This environment has no .NET SDK, Windows PowerShell, DevExpress runtime or DocFX runtime execution path. The Windows .NET 10 rebuild remains the authoritative semantic compilation and documentation-generation test.
