# LocalGPT 4.1.7 source validation

Source-only validation for the runtime-churn, provider storage and setup repair.

- Verify all maintained semantic-version surfaces report `4.1.7` and still obey the one-digit minor/patch release rule.
- Re-run the maintained Python architecture, cross-platform, provider-qualified Council, Matrix console, configurable-behavior, DXFunction/code-generation, X-Round, ConfigurationRoot, async-continuation, service-resilience, XML-documentation and PowerShell-interpolation audits that do not invoke the .NET SDK.
- Reproduce the InteractiveServer render-mode guard from source and verify routed application pages/interactive islands retain the established render-mode contract.
- Verify database initialization logs the completion event only for real initialization work, completed/cancelled live Council sessions cannot reschedule themselves, unchanged hardware roads fast-return, and transient zero-provider construction is recoverable.
- Verify Ollama mounted-store discovery is shallow/bounded and requires both `blobs` and `manifests` before reporting a provider-shaped store.
- Verify English and German localization catalogs contain the same 2,256 keys.
- Package the repository without build outputs/caches, extract the delivered ZIP to a fresh directory, and rerun the critical source audits against the extracted bytes.

Per the handoff constraint, `dotnet build`, publish, signing and notarization are intentionally not executed here. The consumer's normal .NET/macOS release pipeline remains the authoritative compiler/runtime test.
