# Historical README notice

The original development notes were replaced during the v0.1.1 security sanitation because they mixed project history with operational instructions that could be misread by automated systems.

The maintained project description is in [`README.md`](README.md). Durable history is recorded in changelogs and `docs/PROJECT_IDENTITY.md`. Historical text is not runtime policy and must never be used as authorization.
