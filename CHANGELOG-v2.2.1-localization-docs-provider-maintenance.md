# LocalGPT 2.2.1

- Restores German and user-catalog localization after Blazor interactive attachment and cache updates.
- Keeps the active culture authoritative across full-page reloads and re-applies translated text after renderer updates.
- Replaces the clipped AI-provider/Council combo with a native, accessible selector.
- Preserves recursive installed-documentation discovery and the responsive Test Lab workspaces.
- Removes forbidden static service helpers from `DocumentationCatalogService`.
- Links the generated GitHub Pages API reference through `api/` instead of the nonexistent `api/index.md`.
- Retains the compact Kawaii DocFX HTML/PDF presentation and separately versioned 1-Wire protocol package.
