LocalGpt(27) static review
==========================

Application project version: 2.0.2
Review date: 2026-08-02

Scope completed in this source revision
---------------------------------------
- Database-backed Council runtime-class definitions, field ownership, required-human gates,
  keyboard/gamepad binding metadata, source references, service/controller/DXFunction wiring,
  migration and seed data.
- Team-editor runtime-class assignment with complete field previews and categorized best-use
  DXFunction selection.
- DXFunction catalog card namespace grouping and a mass-edit DevExpress grid.
- Source-informed ASCII DOOM and Green Dragon Council presets which extend the existing Council
  workflow rather than adding a separate game engine.
- Exactly one AI member owns each complete ASCII frame/scene. Frame-producing workflow steps reject
  all-members execution when a user saves a team.
- Large, bounded turn steps: ASCII DOOM expands to at most 98 Council rounds; Green Dragon expands
  to at most 97, both below the technical limit of 100.
- Optional, individual installer imports for id-Software/DOOM, lotgd/lotgd, php/doc-en and
  llvm/llvm-project. These examples are not part of the bulk recommended import and are never
  pulled automatically.
- Stable ASCII-frame rendering and reduced structured-text translation work during active Council
  streaming so the interface and Stop action are less likely to be blocked by repeated full-output
  translation.
- Council-team review/save feedback so the save action remains available while idle and explicitly
  explains when confirmation is still required.
- LocalGPT project, seed history, OneWire advertisements and wire-package default version aligned to
  2.0.2. The separate wrapper project was not changed and remains on its existing version.

Static checks performed
-----------------------
- git diff --check: passed (line-ending conversion warnings only).
- Parsed 19 JSON files: passed.
- Parsed 7 XML/project files: passed.
- node --check for localgpt-chat-ui.js, localgpt-reconnect.js and theme-controller.js: passed.
- Lexical delimiter/string/comment review for 40 changed/new C# files: passed.
- Brace review for 9 changed CSS files: passed.
- Merge-marker and focused TODO/FIXME checks: passed.
- ZIP integrity test: passed for the final packaged archive.

Not performed in this environment
---------------------------------
No .NET SDK/compiler or PowerShell runtime is installed here. Therefore restore, compile, EF migration
execution, DevExpress Razor generation and the repository PowerShell assertion scripts were not run.
This archive is a best-effort source revision, not a claimed successful binary build.

Recommended local verification order
------------------------------------
1. Restore and build LocalGPTWebviewWrapper/LocalGPT/LocalGPT.csproj.
2. Start against a copied database and verify migration 20260802010000_AddCouncilRuntimeClasses.
3. Open Council Teams, assign runtime classes, change a team, check the review box and save.
4. Verify the creature-tournament human-collaboration-off configuration still saves and runs.
5. Run ASCII DOOM: confirm one command advances one large world step and one renderer AI emits the
   complete 80x25 frame between the ASCII_FRAME markers.
6. Run Green Dragon: confirm locations/houses, NPCs and events stay in their owned runtime roles and
   one renderer AI emits the complete scene.
7. Start a long Council stream and verify Stop remains responsive and no repeated structured-text
   translation blocks the interface.
8. Test DXFunction card grouping, grid filtering, per-row save and visible/edited mass save.
9. Test each desired knowledge source with a separate installer --repo argument. Do not expect DOOM
   game data/WAD files to be included.
