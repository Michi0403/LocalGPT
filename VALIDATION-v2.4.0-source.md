# LocalGPT 2.4.0 source validation

This package rolls the 2.3.14 source baseline to 2.4.0 while retaining the static-web-asset and CLI build fixes. No compiled output is included.

The first owner-side Windows Debug/Release build should generate the real 2.4.0 DocFX/API/PDF payload and seed the pinned GitHub Pages snapshot from that exact build output.
