# LocalGPT 2.6.6 — Documentation dark contrast + kawaii sprinkle

- Repairs dark-mode contrast for DocFX cards, details, tabs, API member surfaces, neko notes, callouts, headings and footer text.
- Keeps gradient headings but adds a subtle high-contrast text shadow so white labels remain legible over saturated gradients.
- Adds a restrained second star field with slow drift/twinkle over the existing pastel galaxy background.
- Adds tiny delegated hover sprinkles (stars + one paw) for interactive documentation controls without changing button markup or dropdown caret pseudo-elements.
- Honors `prefers-reduced-motion`; hover particles are disabled and the star field becomes static/reduced.
- Synchronizes the maintained DocFX template, embedded help-doc assets and tracked GitHub Pages snapshot cache keys.
- Extends the Kawaii documentation audit so dark-contrast and sparkle markers cannot disappear silently.
- Product projects bumped to 2.6.6. Wire protocol remains 2.1.1.
