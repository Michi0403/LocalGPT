# LocalGPT 4.1.7

LocalGPT 4.1.7 completes the runtime-churn, provider-catalog/storage and setup-interface work recovered from the interrupted 4.1.6 follow-up. The release keeps cancellation authoritative after a stopped Council run, removes repeated idempotent database-initialization completion logging, skips unchanged Council hardware-road normalization, and makes a temporary zero-provider state recoverable instead of tearing down the Blazor circuit.

The setup/runtime workbench now separates provider-catalog availability from optional CanIRun.ai hardware-fit evidence, orders lightweight local model variants first, expands the maintained Ollama alias set, discovers provider-shaped Ollama stores on mounted volumes, and shows documented, inherited, LocalGPT-override, detected and effective storage paths independently. Responsive controls, fixed-height Matrix/setup consoles, and matching English/German evidence strings complete the interface repair.

See `CHANGELOG-v4.1.7-RUNTIME-CHURN-PROVIDER-STORAGE-SETUP.md` and `VALIDATION-v4.1.7-source.md`.
