# LocalGPT 2.0.1 final23 — browser runtime diagnostics

- Corrected the ES-module theme call to invoke the directly exported `applyThemeState` function.
- Made the approvals inbox and Council spooler explicit Interactive Server islands so their click handlers attach.
- Restored a full-width, flexible DX AI Chat host and composer instead of leaving the conversation column squeezed against the left edge.
- Added early browser diagnostics, explicit first-party JavaScript try/catch and console-error reporting, and an interactive .NET logger bridge.
- Added a reviewed JavaScript SHA-256 inventory and build/repository safeguards without changing final19 security or 1-Wire policy files.
- Updated interactive-render and async-continuation baselines only for the reviewed runtime bridges and islands.
