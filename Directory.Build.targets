<Project>
  <!-- Repository validation is intentionally explicit. Build scripts may invoke checks by name,
       but ordinary restore/build/publish operations do not run hidden policy scripts. -->
</Project>
